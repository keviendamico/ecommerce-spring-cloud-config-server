server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          clone-on-start: true
          default-label: main
          uri: https://github.com/keviendamico/ecommerce-spring-cloud-config-repo
          search-paths: config
